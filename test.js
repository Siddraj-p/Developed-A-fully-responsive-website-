// Select the parent element
var parent = document.getElementById('parent');

// Get all child elements with class 'child'
var children = document.getElementsByClassName('parent');

// Access individual children
console.log(parent);
// console.log(children[1].textContent); // Logs "Child 1"
// console.log(children[1].textContent); // Logs "Child 2"

// Alternatively, get the first child element using querySelector
// var firstChild = parent.querySelector('.child');
// console.log(firstChild.textContent); // Logs "Child 1"
// console.log(parent.textnode);